/**
 * Minimal script under top-level `examples/` so `test:integration` can scan this tier.
 * No registry — exits immediately with code 0 (same behavior under `--as-test`).
 */
console.log('examples tier: runnable script placeholder')
